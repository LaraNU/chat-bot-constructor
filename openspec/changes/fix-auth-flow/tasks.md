## 1. Подготовка Supabase (до кода)

- [x] 1.1 Проверить в Authentication → Users аккаунты с незаполненным `email_confirmed_at`; подтвердить вручную или удалить тестовые
- [x] 1.2 Выключить Confirm email в Authentication → Providers → Email
- [x] 1.3 Проверить вручную, что регистрация нового аккаунта возвращает активную сессию (например, через существующую форму — переход пока не работает, но в Users аккаунт появляется подтверждённым)
- [x] 1.4 Создать issue под этот дефект, ветку `fix/auth-flow` от актуального main

## 2. Маппинг ошибок провайдера в shared

- [x] 2.1 Создать `src/shared/lib/supabase/auth-errors.ts` с функцией, отображающей код ошибки Supabase на стабильный ключ: `invalid_credentials`, `email_not_confirmed`, `user_not_found`, `user_already_exists`, `too_many_requests`, `default`
- [x] 2.2 Покрыть unit-тестом: известные коды дают свой ключ, неизвестный и `undefined` дают `default`
- [x] 2.3 Экспортировать через публичный API слайса, если он есть; иначе импортировать по полному пути в соответствии с существующей практикой `shared/lib/supabase`



## 3. Правила доступа к маршрутам в shared

- [x] 3.1 Создать в `src/shared/lib/auth/` чистую функцию, принимающую путь и признак аутентифицированности и возвращающую цель редиректа либо `null`
- [x] 3.2 Реализовать правила: гость на `/editor` → страница входа; аутентифицированный на `/login` и на `/signup` → дашборд; во всех случаях сохраняется префикс локали
- [x] 3.3 Unit-тесты на матрицу правил: обе локали, оба состояния аутентификации, `/login`, `/signup`, `/editor/{id}`, обычная страница. Явный кейс на `/signup` (текущий баг: код сверяется с `/sign-up`) и на сохранение локали
- [x] 3.4 Убедиться, что путь без префикса локали и путь с префиксом обрабатываются одинаково



## 4. Supabase-клиент для middleware

- [x] 4.1 Добавить в `src/shared/lib/supabase/` фабрику клиента для middleware-контекста: cookie читаются из `NextRequest`, записываются в переданный `NextResponse`
- [x] 4.2 Не менять существующие `client.ts` и `server.ts`



## 5. Middleware

- [x] 5.1 Переписать `src/middleware.ts`: сначала `intlMiddleware(request)` формирует ответ, затем middleware-клиент Supabase пишет в него cookie, затем `getUser()`
- [x] 5.2 Заменить проверку `cookie.name.includes('auth-token')` на результат `getUser()`
- [x] 5.3 Применить решение из чистой функции (задача 3.1); при редиректе перенести обновлённые cookie на объект редирект-ответа
- [x] 5.4 Убедиться, что `matcher` не изменился и по-прежнему исключает `api` (rate limiting webhook живёт в контроллере — см. P1.2)
- [x] 5.5 Прогнать вручную: гость на `/ru/editor/{id}` → `/ru/login`; аутентифицированный на `/en/signup` → `/en`



## 6. Хук регистрации

- [x] 6.1 Создать `src/features/sign-up-form/model/use-sign-up.ts` по образцу `use-sign-in.ts`: схема валидации, `useForm`, состояние загрузки, сабмит
- [x] 6.2 Использовать маппинг ошибок из задачи 2.1 и переводы из namespace `SignUpForm.errors.*`; показывать ошибку через `toast`, убрать `console.error`
- [x] 6.3 Убрать опцию `emailRedirectTo` из вызова `signUp` — письмо не отправляется
- [x] 6.4 После успеха выполнить `push` на дашборд и следом `refresh`, роутер брать из `@/i18n/navigation`
- [x] 6.5 Переключить `sign-up-form.tsx` на хук: компонент перестаёт напрямую работать с Supabase и хранить схему валидации
- [x] 6.6 Сохранить существующие `data-testid` (`user-name-input`, `user-email-input`, `user-password-input`, `submit-sign-up-form`) — на них завязаны Playwright-тесты



## 7. Хук входа

- [x] 7.1 Добавить в `use-sign-in.ts` навигацию после успеха: `push` + `refresh`, роутер из `@/i18n/navigation`
- [x] 7.2 Перевести `getAuthErrorMessage` на общий маппинг из задачи 2.1, сохранив тексты из namespace `SignInForm.errors.*`
- [x] 7.3 Убедиться, что состояние загрузки снимается на любом исходе



## 8. Переводы

- [x] 8.1 Добавить в `src/shared/langs/en.json` ветку `SignUpForm.errors.*` с полным набором ключей из задачи 2.1
- [x] 8.2 Добавить зеркальную RU-версию в `src/shared/langs/ru.json`, ключи 1:1 с EN
- [x] 8.3 Сверить набор ключей `SignInForm.errors.*` с новым маппингом, дописать недостающие
- [x] 8.4 Прогнать `npx tsc --noEmit`



## 9. Шапка приложения

- [x] 9.1 В `src/widgets/header/ui/header.tsx` добавить `asChild` кнопке входа, убрав вложенность `<button><a>`
- [x] 9.2 Убрать лишний `type="button"` с обеих ссылок внутри кнопок
- [x] 9.3 Проверить клавиатурную доступность обеих кнопок (Tab, Enter)



## 10. Тесты

- [x] 10.1 Unit-тест на `use-sign-up`: успех вызывает навигацию; ошибка вызывает `toast` с сообщением по ключу и не вызывает навигацию
- [x] 10.2 Unit-тест на `use-sign-in`: те же два случая
- [x] 10.3 Дописать `tests/signup-page.spec.ts`: успешная регистрация ведёт на `/(en|ru)/?$`
- [x] 10.4 Понизить таймаут ожидания URL в `tests/login-page.spec.ts` и `tests/auth.setup.ts` до значения, при котором прежняя гонка падала бы
- [ ] 10.5 Прогнать `npm run test`, `npm run lint`, `npm run test:playwright`



## 11. Документация

- [x] 11.1 Добавить в `docs/cursor-context.md` → «Temporary / Provisional Decisions» пункт о выключенном подтверждении почты с условием отката в change `transactional-email`
- [x] 11.2 Привести раздел про аутентификацию в `README.md` в соответствие с фактическим поведением: подтверждение почты временно выключено, восстановления пароля пока нет
- [x] 11.3 Отметить P1.4 в `docs/roadmap.md` и `docs/roadmap-ru.md` как Done (валидация сессии в middleware закрывается этим change)



## 12. Проверка на проде

- [x] 12.1 Регистрация нового аккаунта → попадание на дашборд
- [x] 12.2 Выход → неаутентифицированная главная с рабочими кнопками входа и регистрации
- [x] 12.3 Повторный вход тем же аккаунтом → дашборд
- [x] 12.4 Гость открывает `/editor/{id}` напрямую → страница входа
- [x] 12.5 Повторить основной сценарий на второй локали